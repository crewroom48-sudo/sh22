workflows:
  expo-android:
    name: Expo Android Build

    environment:
      node: latest

      groups:
        - expo

    scripts:
      - npm install
      - npm install -g eas-cli
      - eas build --platform android --non-interactive

    artifacts:
      - "*.apk"
      - "*.aab"
